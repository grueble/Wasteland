#ifndef _SCENE_HPP_
#define _SCENE_HPP_

// create functions such as
// getObjs
// getScene
// etc., these are called by the top-level to load up PhysObj and render textures at the start of a scene

// include GameObjects, and use them to populate the various scenes, which are really just lists of objects

// getPhysObjs(std::vector<PhysObj>& phys_objs);

// getActors();

#endif